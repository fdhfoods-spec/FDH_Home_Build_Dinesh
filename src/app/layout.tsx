import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fresh Direct Home | Fresh. Trusted. Delivered.",
  description:
    "Fresh Direct Home is a premium farm-to-home marketplace for meat, seafood, fruits, vegetables, dairy, groceries, and verified home chef meals.",
  keywords: [
    "fresh groceries",
    "farm to home delivery",
    "verified local vendors",
    "home chef meals",
    "scheduled grocery delivery",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
